{
  "name": "decap-oauth-bridge",
  "private": true,
  "version": "0.0.1",
  "description": "Cloudflare Worker that brokers GitHub OAuth for the production /admin/ Decap CMS.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251101.0",
    "typescript": "^5.6.0"
  }
}
